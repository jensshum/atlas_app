name: siri2_app
description: "Flutter app for controlling the Atlas AI phone agent."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  shared_preferences: ^2.3.0
  provider: ^6.1.2
  network_info_plus: ^6.0.0
  permission_handler: ^11.3.0
  intl: ^0.19.0
  record: ^6.0.0
  audioplayers: ^6.1.0
  google_fonts: ^6.2.1
  flutter_dotenv: ^5.2.1
  flutter_local_notifications: ^18.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
